import 'package:flutter/material.dart';

class EkinchiBarak extends StatelessWidget {
  const EkinchiBarak({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}